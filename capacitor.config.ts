import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'one.aysin.gencode',
  appName: 'gencode',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
